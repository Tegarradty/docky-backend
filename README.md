# DOCKY Backend API

## Persiapan Sandbox (Penting!)
Sebelum menjalankan API, Anda **wajib** membuat image Docker lokal untuk ruang isolasi eksekusi kode.
Jalankan perintah ini di terminal Anda:

`docker build -t docky-sandbox -f docker/Dockerfile.sandbox .`